% !TEX encoding = UTF-8 Unicode
\documentclass[tikz]{standalone}
\usepackage{sansmathfonts}			% Sans Serif equations
\renewcommand*\familydefault{\sfdefault} 	% Sans Serif as default font
\usepackage{pgfplots}
\usepackage{grffile}

\begin{document}
%Fig. 1
\pgfplotstableread[col sep = comma]{data.csv}\data
\begin{tikzpicture}
	\begin{axis}[grid=both, ymin=-1, ymax= 1, xmin=0, xmax=2*pi,
		width=10cm, height=6cm, tick label style={font=\scriptsize},  label style={font=\scriptsize},
		xlabel={$\omega t$}, legend style={font=\tiny}, legend pos=north east,
		xtick={0,pi/2,pi,3/2*pi,2*pi},
		xticklabels={{$0$},{$\frac{\pi}{2}$},{$\pi$},{$\frac{3}{2}\pi$},{$2\pi$}}]
		\addplot[color=blue, thick] table[x index=0, y index=1]{\data};
		\addplot[color=red, thick] table[x index=0, y index=2]{\data};
		\legend{$\sin\left(\omega t\right)$,$\cos\left(\omega t\right)$}
	\end{axis}
\end{tikzpicture}
\end{document}
